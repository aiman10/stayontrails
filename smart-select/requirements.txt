flask>=3.0
numpy>=1.26
opencv-python>=4.9
ultralytics>=8.3
# torch is pulled in by ultralytics. For GPU, install the CUDA build first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
